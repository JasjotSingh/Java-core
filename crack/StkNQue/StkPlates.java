
class Solution{
  1. create an linkedlist/arraylist of stacks.
  2.push elem
    2a. if stk.size() == limit size. create new stack.
    2b. add val in new stk
    2b. is stk.size() < limit size. add elem in last stack.
  3.pop elem
    3a. id stk.size() == 0. remove last

}

class StkPlates{
  
}
